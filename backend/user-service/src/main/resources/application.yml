spring:
  application:
    name: user-service
  jackson:
    serialization:
      indent_output: true

server:
  port: 8080

security:
  jwt:
    secret: ${JWT_SECRET:change-me}

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
