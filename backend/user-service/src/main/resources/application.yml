spring:
  application:
    name: user-service
  jackson:
    serialization:
      indent_output: true

server:
  port: 8080

security:
  jwt:
    secret: ${JWT_SECRET:0123456789abcdef0123456789abcdef}

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
